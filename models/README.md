# Modelos

El entrenamiento genera `churn_pipeline.joblib` en esta carpeta. El archivo incluye el preprocesamiento y el modelo, no se versiona con Git y también queda registrado como artefacto en MLflow.
